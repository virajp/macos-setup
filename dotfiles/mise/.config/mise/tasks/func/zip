#!/usr/bin/env zsh
#MISE hide=true

set -e
source "${0%/*}/_helpers"

# Display progress message with colored output
print_header "Zipping $1 to $1.zip ..."

# Create recursive zip archive
zip --recurse-paths "$1.zip" "$1"

print_success "Backup completed: $1.zip"
