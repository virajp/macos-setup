#!/usr/bin/env bash
#MISE description="Start the mempalace stack via pitchfork"
#MISE depends=["mempalace:check"]

set -e
source "${HOME}/.config/mise/tasks/_scripts/_helpers"

print_header "Starting qdrant backend ... "
docker compose --file "${HOME}/.config/mempalace/docker-compose.yaml" up --detach

print_subheader "Starting mempalace ..."
# Clear any disable left by mempalace:stop, which guards the on_exit hook.
pitchfork enable mempalace-mcp
pitchfork start mempalace-mcp
