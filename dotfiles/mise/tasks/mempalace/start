#!/usr/bin/env bash
#MISE description="Start the qdrant (mempalace) stack"
#MISE depends=["mempalace:check"]
#MISE dir="~"

set -e
source "${HOME}/.config/mise/tasks/_scripts/helpers"

print_header "Starting qdrant (mempalace) ... "
mise bootstrap compose apply --yes --silent
