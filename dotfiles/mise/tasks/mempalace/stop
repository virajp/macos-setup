#!/usr/bin/env bash
#MISE description="Stop the mempalace stack"

set -e
source "${HOME}/.config/mise/tasks/_scripts/helpers"

print_header "Stopping qdrant (mempalace) ..."
docker compose --file "${HOME}/.config/mempalace/docker-compose.yaml" down
