# @askviraj/linter configuration
#
# Add custom ignores, rule overrides, and additional config blocks below.

version: 1

ignores:
  # Written by `gh` itself through the symlink — not ours to format.
  - "dotfiles/github/hosts.yml"

#
# overrides:
#   typescript:
#     rules:
#       "@typescript-eslint/no-explicit-any": "error"
#
# configs:
#   - name: "custom"
#     files: ["**/*.ts"]
#     rules: {}
