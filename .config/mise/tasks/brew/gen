#!/usr/bin/env bash
#MISE description="Generate Brewfile"
#MISE hide=false

set -e
source "${MISE_PROJECT_ROOT}/.config/mise/tasks/_scripts/_helpers"

print_header "Generating Brewfile ..."
brew bundle dump \
  --force \
  --describe \
  --file "./brewfile.gen"

print_header "Printing the diff ... "
diff -u ~/brewfile brewfile.gen | diff-so-fancy
